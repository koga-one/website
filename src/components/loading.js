import * as React from "react";

const Loading = () => {
  return (
    <div className="absolute left-0 flex h-full w-full bg-kami font-fira text-h3 font-black tablet:text-h1">
      <p className="mx-auto self-center">Loading...</p>
    </div>
  );
};

export default Loading;
